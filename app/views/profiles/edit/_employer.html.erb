<h1>Edit Company</h1>

<%= simple_form_for(@profile, as: 'profile', url: profile_path, html: {class:"custom"}) do |f| %>
  <div class="form-inputs">
    <%= f.input :company_name %>
    <%= f.input :website %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>